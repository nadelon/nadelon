<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- AppCompat parent so MediaRouteButton can inflate; Compose overrides visuals. -->
    <style name="Theme.Nadelon" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
